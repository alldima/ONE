name: CI test 1

on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: build
      run: |
        mkdir nnfw_build
        cd nnfw_build
        sudo apt-get install libboost-all-dev
        cmake -DCMAKE_BUILD_TYPE=Debug ../infra/nnfw
    #       cd tests/nnfw_api
    #       make
    #       ./nnfw_api_gtest --gtest_filter=TestWhileDynamicModelLoaded.run_verify
 
     #- uses: actions/download-artifact@v3
     #   with:
     #     name: my-artifact
     #     path: # optional
    
     # - name: Display structure of downloaded files
     #   run: ls -R
